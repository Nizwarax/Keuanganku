name: Android CI Build

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    name: Build Release APK
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'gradle'

    - name: Decode Debug Keystore (Bypass Key)
      run: |
        if [ -f debug.keystore.base64 ]; then
          base64 -d debug.keystore.base64 > debug.keystore
        else
          echo "debug.keystore.base64 not found, Gradle will auto-generate debug key"
        fi

    - name: Create fallback .env file
      run: |
        if [ ! -f .env ]; then
          cp .env.example .env
        fi

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v4
      with:
        gradle-version: '9.3.1'

    - name: Build Release APK (Bypassed Signing)
      run: gradle :app:assembleRelease

    - name: Upload Release APK
      uses: actions/upload-artifact@v4
      with:
        name: Keuanganku-Release-APK
        path: app/build/outputs/apk/release/*.apk
        if-no-files-found: error
